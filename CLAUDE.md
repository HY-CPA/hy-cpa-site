# 山田祥子公認会計士・税理士事務所 サイト

山田祥子公認会計士・税理士事務所（略称 HY-CPA）の公式ホームページ。
コンセプトは「難しいを、体感できる形に。」（20年の公認会計士・監査経験を土台に、
会計・税務・ガバナンスを専門家ではない当事者が体感できる形にする）。
訴求構成は 土台（公認会計士・監査20年）／手法（監査DX）／表現（ゲーム・映像・ツール）。
プロフィール・活動紹介・IPOクエスト・お問い合わせに絞った名刺代わりのサイト。
映像（MV）はリンクできる実績ができるまで works には載せない（2026-09-15 修正案 v4）。

## 掲載方針（2026-09-14 決定）
- サービス提供（税務顧問、記帳・決算・申告、権利収入の税務 など税務の専門性）は
  うたわない。代表は常勤監査役に注力しており継続サービスは提供していない。
- AIコンテンツ制作の実績は載せない。制作実績は IPOクエストのみ。
- 常勤監査役先の社名は上場承認まで伏せる（「上場準備中の企業」「エンタメ領域の上場準備企業」）。
- HY-CPA はロゴ・ドメインの略称として残す（ヘッダーの brand__mark）。
- 研修・講演・教育活動は積極的に行っていないため、ページ末尾の「ご相談」CTA 帯や
  お問い合わせへ誘導するボタンは置かない（2026-09-16）。お問い合わせページ自体は残す。
- セミナー登壇・IPOクエストを使った体験型セミナーの実績は現時点でない。
  実績ができるまで「登壇しています」「実施しています」「セミナー参加者向けに提供」等は書かない。

## 技術方針
- ビルド不要の静的サイト（HTML + CSS + 最小限の JS）。フレームワーク・npm 依存なし。
- 公開先は GitHub Pages（リポジトリ HY-CPA/hy-cpa-site、main ブランチのルート）。
  独自ドメイン https://www.hy-cpa-office.com/ を割り当て済み（2026-09-15）。
  リポジトリ直下の CNAME ファイルがその設定なので削除しないこと。
  DNS は Squarespace Domains で管理（www の CNAME → hy-cpa.github.io、apex の A レコード4本、
  所有確認の TXT）。メール用の MX・SPF・DKIM には触れない。
- 研修ゲーム「IPOクエスト」の本体は Private リポジトリ HY-CPA/ipo-quest で管理する資産。
  この Public リポジトリには、暗号化済みであってもゲーム本体・配置スクリプトを置かない
  （紹介文とお問い合わせ導線のみ掲載する）。

## 構成
- index.html            トップ
- about.html            事務所案内・プロフィール・登録・事務所概要
- works.html            制作実績（IPOクエストのみ）
- activities.html       活動（租税教室・確定申告相談会・登壇）
- news.html             お知らせ一覧
- contact.html          お問い合わせ（Google フォーム＋メール）
- privacy.html          プライバシーポリシー
- css/style.css         共通スタイル。色・フォントは :root の変数で管理
- js/main.js            モバイルナビ開閉のみ
- images/               画像（未使用。写真・スクリーンショットはここに置く）
- robots.txt            仮公開中は全拒否（Claude-User のみ許可。チャットの Claude が読めるように）
- CNAME                 独自ドメイン設定（www.hy-cpa-office.com の1行）
- docs/                 内部検討メモ・ドラフト置き場（.gitignore 済み。コミットしない）

## 編集ルール
- ヘッダー・フッター・ナビは各 HTML に直書きしている（ビルド不要を優先）。
  ナビ項目を変えるときは全 HTML を同時に修正する。
- 各ページのナビで現在ページに `aria-current="page"` を付ける。
- お知らせ追加：news.html の `<ul class="news-list">` 先頭に `<li>` を追加し、
  index.html の NEWS 欄にも最新3件を反映する（古いものは index から外す）。
- 仮置きの文言は `<span class="placeholder">要確認：…</span>` で示している。
  実際の情報に差し替えたら span ごと削除する。残りは
  `grep -n "placeholder" *.html` で一覧できる。
- 外部 CSS/JS/フォント/解析タグは原則入れない。入れるなら privacy.html の
  「7. Cookie・アクセス解析」を書き換え、「3. 利用目的」に統計的分析の項目を追加し、
  改定日を更新する。外部サービスを増やしたら「6. 外部サービスの利用」にも追記する。

## 仮公開 → 本公開のチェックリスト
1. 全 HTML から `<meta name="robots" content="noindex, nofollow">` を削除
2. robots.txt の `User-agent: *` を `Allow: /` に変更（Claude-User の個別指定は削除してよい）
3. placeholder が残っていないことを確認
4. contact.html のフォーム URL・メールアドレスが本番のものか確認

## 公開・確認
- ローカル確認：`python3 -m http.server 8000` をリポジトリ直下で実行し
  http://localhost:8000/ を開く（file:// 直開きでも動く）。
- main に push すれば GitHub Pages に自動反映（数十秒〜数分）。
- 変更後は少なくともトップ・変更したページ・モバイル幅の表示を確認する。
